#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;
    int N,M;
    for (int i = 0 ;i < T;i++)
    {
        cin >> N >> M;
        int A[N][M];
        for (int j = 0;j < N;j++)
            for (int k = 0;k < M;k++)
                cin >> A[j][k];
        cout << Max_Unit_Area(int N,int M,int )
    }

}