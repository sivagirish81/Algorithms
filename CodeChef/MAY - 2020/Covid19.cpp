#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;
    int N;
    vector<int> X;
    int tmp;
    for (int i = 0;i < T;i++)
    {
        X.clear();
        for (int i = 0;i < N;i++)
        {   
            cin >> tmp;
            X.push_back(tmp);
        }
        
}